#include <Problem/State.hh>

using namespace Problem;

__device__
State::State(Type type, std::size_t sizeStorage, std::byte * const storage) :
    type(type),
    sizeStorage(sizeStorage),
    storage(storage)
{}