#pragma once

#include <CustomTemplateLibrary/CTL.hh>

class Variable
{
    public:
        int const minValue;
        int const maxValue;

    public:
        Variable(int minValue, int maxValue);
};
